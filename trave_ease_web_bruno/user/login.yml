info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/users/login/
  body:
    type: json
    data: |-
      {
      "username": "asistente",
      "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
